apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mgmt-proxmox-cluster-testlab
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: mgmt
  ignoreDifferences: []
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
  source:
    repoURL: https://github.com/Dayzpd/kustomize-proxmox-cluster
    targetRevision: HEAD
    path: overlays/testlab
  destination:
    name: in-cluster